/** User message that triggers a full quiz rewrite to match global quiz house rules. */
export const APPLY_QUIZ_RULES_MESSAGE =
  "Rewrite the quiz in body_md so it fully satisfies the GLOBAL QUIZ HOUSE RULES block in your instructions. Emit the complete updated quiz in the JSON update block.";
