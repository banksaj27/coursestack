/** User message that triggers a full pset rewrite to match global problem set house rules. */
export const APPLY_PROBLEM_SET_RULES_MESSAGE =
  "Rewrite the problem set in body_md so it fully satisfies the GLOBAL PROBLEM SET HOUSE RULES block in your instructions. Emit the complete updated assignment in the JSON update block.";
